import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class RadiusUtility {
  static BorderRadius radiusFive = const BorderRadius.all(Radius.circular(5)).r;
  static BorderRadius radiusTen = const BorderRadius.all(Radius.circular(10)).r;
  static BorderRadius radiusTwenty= const BorderRadius.all(Radius.circular(20)).r;
}
