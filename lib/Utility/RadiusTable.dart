import 'package:flutter/material.dart';

class RadiusTable {
  static BorderRadius radiusFive = const BorderRadius.all(Radius.circular(5));
  static BorderRadius radiusTwenty= const BorderRadius.all(Radius.circular(20));
}
